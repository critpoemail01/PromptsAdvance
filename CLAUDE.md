@AGENTS.md

# Claude Code

Segue `AGENTS.md` como instrução comum. Quando existir `LIFECYCLE_STATE.json`,
usa a skill equivalente ao lifecycle, executa `status` e `validate` e trabalha
apenas em `NEXT_TASK.md`. Lê integralmente cada documento referenciado pelo
prompt atual; não carregues todo o catálogo sem necessidade.
